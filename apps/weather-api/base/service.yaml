apiVersion: v1
kind: Service
metadata:
  name: weather-api
  labels:
    app: weather-api
spec:
  type: ClusterIP
  selector:
    app: weather-api
  ports:
    - name: http
      port: 8000
      targetPort: http
