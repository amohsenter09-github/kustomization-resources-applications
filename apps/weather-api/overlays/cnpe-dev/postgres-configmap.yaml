apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres
  labels:
    app: postgres
data:
  POSTGRES_USER: weather
  POSTGRES_PASSWORD: weather
  POSTGRES_DB: weather
  PGDATA: /var/lib/postgresql/data/pgdata
