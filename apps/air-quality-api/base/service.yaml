apiVersion: v1
kind: Service
metadata:
  name: air-quality-api
  labels:
    app: air-quality-api
spec:
  type: ClusterIP
  selector:
    app: air-quality-api
  ports:
    - name: http
      port: 8001
      targetPort: http
